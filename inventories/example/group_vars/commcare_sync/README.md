vault.yml
=========

"vault_*" variables are stored in the `vault.yml` file. View the content
with

```shell
$ ansible-vault view vault.yml
```

Use password "secret".

See `docs/production.md` for instructions on creating a real `vault.yml`
file.
