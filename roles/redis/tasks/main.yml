---

- name: Install Redis
  apt:
    name: redis-server
    update_cache: "{{ apt_update_cache }}"
    state: present
  tags:
    - provision
    - packages
    - redis

- name: Ensure Redis is running
  service:
    name: redis-server
    state: started
    enabled: yes
  tags:
    - provision
    - redis
